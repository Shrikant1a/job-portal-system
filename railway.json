{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "java -cp bin:lib/* com.jobportal.application.ApiServer",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
